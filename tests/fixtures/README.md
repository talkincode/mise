# Test Fixtures

This directory contains test fixtures for mise golden tests.

## Structure

- `sample_project/` - A minimal project structure for testing
  - Contains sample files with anchors
  - Used for scan, extract, and anchor tests

## Adding New Fixtures

1. Create a new directory under `fixtures/`
2. Add the necessary files
3. Update the golden tests in `tests/golden.rs`
