# tests/samples

这个目录用于 `mise` 的端到端测试素材。

- `sample_project_alpha/`：多语言/多层目录/Anchor/匹配与提取场景。
- `sample_project_beta/`：包含 `.gitignore`、hidden 文件与 ignore/hidden 参数覆盖。
- `anchors_invalid/`：故意构造的 Anchor lint 负例（用于验证 lint 输出）。
